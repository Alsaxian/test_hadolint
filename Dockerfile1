# Start from the latest Ubuntu image
FROM ubuntu:latest

# Install the cowsay package from apt
RUN apt-get update && apt-get install -y cowsay

# Run the cowsay command when the image is run
CMD ["cowsay", "Hello, world!"]
